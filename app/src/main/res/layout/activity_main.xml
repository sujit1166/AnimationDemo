<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:id="@+id/rlRoot"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_background"
    android:paddingVertical="20dp"
    tools:context=".MainActivity">

    <RelativeLayout
        android:id="@+id/rlCircularWithUsers"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/gradient_background"
        android:paddingVertical="20dp"
        android:visibility="gone"
        tools:context=".MainActivity">

        <include layout="@layout/partial_layout_multiple_users_onboarding" />
    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/rlLanguageSelection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnShare"
        android:background="@drawable/gradient_background"
        tools:context=".MainActivity">

        <include layout="@layout/partial_layout_language_selection" />
    </RelativeLayout>

    <Button
        android:id="@+id/btnShare"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="20dp"
        android:background="@drawable/buttonstyle"
        android:text="Next"
        android:textColor="#fff" />


</RelativeLayout>